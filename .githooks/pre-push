#!/usr/bin/env bash
set -euo pipefail

if [[ "${TIMERTAB_SKIP_GIT_HOOKS:-}" == "1" ]]; then
  exit 0
fi

repo_root="$(git rev-parse --show-toplevel)"
cd "${repo_root}"

go vet ./...
go build ./cmd/timertab
go test ./...
